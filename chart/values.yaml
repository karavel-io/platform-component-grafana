publicURL:

secret:
  store:
    name: default
    kind: ClusterSecretStore
  key:

operator:
  image: quay.io/grafana-operator/grafana-operator
  tag: v4.2.0

grafana:
  image: docker.io/grafana/grafana
  tag:

dex:
  enable: false
  name: dex
  namespace: dex
  issuer:
